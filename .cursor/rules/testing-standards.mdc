---
description: 测试失败时的修复决策规范：先审查测试，再决定改代码还是改测试。对于 UI/交互验证，优先使用 MCP 浏览器工具进行手动/半自动验证。
alwaysApply: true
---

---

# 测试失败修复决策规范

## 核心原则

> **永远不要为了使测试通过而直接修改测试断言。**

当遇到测试失败时，必须遵循以下决策流程：

> **UI/交互验证优先方案**：对于视觉、布局、交互相关的验证，优先使用 MCP 浏览器工具（`cursor-ide-browser`）进行手动/半自动验证，而非 Playwright 自动化测试。MCP 工具提供更灵活的实时验证能力。

## 修复决策流程

```
测试失败
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 1：理解测试意图                                            │
│ - 这个测试想验证什么业务行为？                                   │
│ - 测试期望的结果是否有���品需求支撑？                             │
│ - 查看需求文档、PRD、用户故事、设计稿                           │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 2：审查测试本身的正确性                                    │
│ - 测试期望的行为是否符合真实需求？                               │
│ - 测试是否硬编码了不必要的实现细节？                             │
│ - 测试是否基于过时的需求？                                      │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
    ├─── 测试正确（符合真实需求）────────────────────────────────► 修改实现代码
    │       （代码有 bug 或不符合需求）                            │
    │                                                              │
    └─── 测试错误（不符合真实需求）──────────────────────────────► 修改测试
            （测试本身有误）                                      │
```

## 审查清单

### 测试符合真实需求的迹象

- [ ] 测试覆盖了明确的业务需求或用户故事
- [ ] 测试期望的行为在产品文档/设计稿中有明确说明
- [ ] 测试覆盖了边界情况和异常场景
- [ ] 测试命名清晰表达了意图（如 `should throw error when input is empty`）
- [ ] 测试断言的是业务行为，而非实现细节

### 测试不符合真实需求的迹象

- [ ] 测试硬编码了实现细节（如具体的 CSS 值、内部变量名、魔法数字）
- [ ] 测试期望的行为与产品需求相矛盾
- [ ] 测试基于过时的需求或已被废弃的功能
- [ ] 测试过于具体，无法适应合理的实现变化
- [ ] 测试期望值与当前实际业务逻辑不一致

## 具体场景处理

### 场景 1：E2E/UI 测试失败（CSS 样式、布局）

```
❌ 错误做法：直接修改测试期望的 CSS 值
✅ 正确做法：
   1. 检查设计稿/UI 规范，确认正确的样式值
   2. 如果规范中要求的是测试期望的值 → 修改组件 CSS
   3. 如果规范中要求的是当前实现的值 → 更新测试期望
   4. 如果是响应式/动态样式 → 修改测试为容错检查
```

#### MCP 浏览器工具优先验证流程

对于 UI/交互相关的问题，建议优先使用 MCP 工具进行实时验证：

```
1. 启动服务
   - Angular: http://localhost:4200

2. 使用 MCP 工具验证
   browser_navigate → 导航到页面
   browser_snapshot → 获取页面结构
   browser_take_screenshot → 截图对比
   browser_click/browser_fill → 交互测试

3. 验证步骤
   - 检查 Angular 界面的视觉正确性
   - 检查交互行为是否一致
   - 验证动画、过渡效果
   - 检查控制台错误

4. 记录结果
   - 截图保存到 docs/screenshots/
   - 记录不一致项到 GitHub Issue

5. 修复决策
   - 如果确认是实现 bug → 修改组件代码
   - 如果测试期望不符合实际需求 → 更新测试
```

**MCP 工具适用场景**：
- 视觉一致性对比（截图对比）
- 交互行为验证（点击、输入、拖放）
- 动画效果检查（悬停、过渡、加载）
- 响应式布局测试（不同窗口尺寸）
- 控制台/网络请求检查

### 场景 2：单元测试失败（函数返回值、状态）

```
❌ 错误做法：修改测试断言使其通过
✅ 正确做法：
   1. 检查函数的功能需求文档
   2. 确认期望返回值是否有业务依据
   3. 如果业务逻辑要求不同的返回值 → 修改实现
   4. 如果测试期望不符合业务逻辑 → 修改测试
```

### 场景 3：集成测试失败（API 响应、数据库状态）

```
❌ 错误做法：修改测试的期望响应/状态
✅ 正确做法：
   1. 检查 API 契约/数据库 schema
   2. 确认期望的响应格式是否正确
   3. 如果契约要求不同的格式 → 修改 API/Repository
   4. 如果测试使用了过时的契约 → 更新测试
```

### 场景 4：测试环境问题（超时、文件缺失、网络错误）

```
✅ 正确做法：
   1. 区分"测试逻辑问题"和"测试环境问题"
   2. 环境问题：修复环境（创建缺失文件、启动服务）
   3. 如果环境无法修复：添加测试跳过逻辑并记录原因
   4. 不要因为环境问题就修改测试的业务断言
```

## 禁止行为

- ❌ **禁止**：为了使测试通过而修改测试断言
- ❌ **禁止**：添加 `test.skip()` 或 `pending` 来掩盖真实的测试失败
- ❌ **禁止**：修改测试的期望值以匹配错误的实现
- ❌ **禁止**：在未审查测试正确性的情况下修改代码

## 强制检查点

在修改任何测试之前，必须回答以下问题：

1. **这个测试覆盖了什么需求？**（引用需求文档或用户故事）
2. **测试期望的行为是产品应该有的行为吗？**
3. **如果我不改测试，产品行为会变成什么样？**
4. **修改测试是否会降低测试的保护作用？**

如果无法明确回答以上问题，**停止修改，先进行需求确认**。

## 记录要求

每次修复测试失败时，必须在提交信息中说明：

```
fix: 修复 XXX 功能

测试失败原因：测试期望 Y，但实际返回 Z
决策过程：
- 检查需求文档 XXX，确认期望行为应为 Y
- 当前实现返回 Z，不符合需求
- 修改实现代码（文件路径），使其返回 Y

测试未修改，因为测试期望符合产品需求。
```

或

```
fix: 修复 XXX 测试

测试失败原因：测试期望 Y，但产品实际行为是 Z
决策过程：
- 检查需求文档 XXX，确认产品行为应为 Z
- 测试期望 Y 不符合当前产品需求
- 修改测试期望值（文件路径），从 Y 改为 Z

原因：测试基于过时的需求/设计。
```

## 多服务架构调试规范

### 后端 404 错误调试流程

当多服务架构中遇到 404 错误时，不要假设"后端未运行"，按以下流程排查：

```
┌────────────────────────────────────────────────────────────────┐
│ 步骤 1：验证后端是否真正运行                                     │
│ - 检查进程：ps aux | grep -E "(uvicorn|fastapi)"               │
│ - 直接请求：curl -I http://localhost:PORT/endpoint             │
│ - 确认不是"后端未运行"的误判                                    │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 2：检查实际 HTTP 请求 URL                                  │
│ - 打开浏览器 DevTools → Network 标签                           │
│ - 找到失败的请求，记录完整 URL                                  │
│ - 注意：URL 可能被 proxy.conf.json 改写                        │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 3：对比三方路径一致性                                       │
│ Frontend Service URL → proxy.conf.json → Backend Route        │
│ 三者必须完全对齐！                                              │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 4：检查 CORS 响应头                                        │
│ curl -I -H "Origin: http://localhost:4200" http://localhost:PORT/ │
└────────────────────────────────────────────────────────────────┘
```

### 快速调试命令

```bash
# 检查后端进程
ps aux | grep -E "(uvicorn|fastapi)" | grep -v grep

# 直接测试后端路由（最关键！）
curl -I http://localhost:PORT/endpoint

# 检查 CORS 响应头
curl -I -H "Origin: http://localhost:4200" http://localhost:PORT/endpoint

# 查看 proxy 配置
cat apps/web-angular/proxy.conf.json
```

### URL 路径对齐检查表

| 服务 | 端口 | 前端调用 | Proxy 目标 | Backend 路由 |
|------|------|---------|------------|-------------|
| AI Agents | 8000 | /api/agents/* | http://localhost:8000/* | /agents/* |
| RAG | 8002 | /api/rag/* | http://localhost:8002/* | /rag/* |
| TTS | 8001 | /api/speech | http://localhost:8001/tts | /tts |
| Vision | 8003 | /api/vision | http://localhost:8003/vision | /vision |

### 常见错误诊断表

| 症状 | 可能原因 | 诊断方法 | 解决方案 |
|------|---------|---------|---------|
| 404 | 后端未运行 | `curl -I http://localhost:PORT/` | 启动后端 |
| 404 | 路径不匹配 | 对比 frontend/proxy/backend 三方 | 统一路径 |
| CORS | origin 未列入 | `curl -I -H "Origin: ..."` | 添加到 allowed_origins |
| 502 | proxy target 错误 | 检查端口 | 修正 proxy.conf.json |

### Angular/Python 后端集成要点

1. **proxy.conf.json 必须与 backend 路由对齐**
   - Frontend: `/api/speech` → Proxy: `http://localhost:8001/tts`
   - Backend: `@app.post("/tts")`

2. **CORS 配置必须包含所有前端端口**
   - Angular dev: `http://localhost:4200`

3. **验证顺序**：curl 测试 → 浏览器 DevTools → 对比三方配置
