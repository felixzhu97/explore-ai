---
description: 测试失败时的修复决策规范：先审查测试，再决定改代码还是改测试。对于后端/集成验证，优先使用接口契约对照与直接请求验证。
alwaysApply: true
---

# 测试失败修复决策规范

## 核心原则

> **永远不要为了使测试通过而直接修改测试断言。**

当遇到测试失败时，必须遵循以下决策流程：

> **后端/集成验证优先方案**：对于 API、服务集成、数据流等验证，优先使用直接请求/日志/契约对照验证，而非前端 UI 自动化测试。

## 修复决策流程

```
测试失败
    │
    ▼
┌────────────────────────────────────────────��───────────────────┐
│ 步骤 1：理解测试意图                                            │
│ - 这个测试想验证什么业务行为？                                   │
│ - 测试期望的结果是否有业务需求支撑？                             │
│ - 查看需求文档、PRD、用户故事                                   │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 2：审查测试本身的正确性                                    │
│ - 测试期望的行为是否符合真实需求？                               │
│ - 测试是否硬编码了不必要的实现细节？                             │
│ - 测试是否基于过时的需求？                                      │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
    ├─── 测试正确（符合真实需求）────────────────────────────────► 修改实现代码
    │       （代码有 bug 或不符合需求）                            │
    │                                                              │
    └─── 测试错误（不符合真实需求）──────────────────────────────► 修改测试
            （测试本身有误）                                      │
```

## 审查清单

### 测试符合真实需求的迹象

- [ ] 测试覆盖了明确的业务需求或用户故事
- [ ] 测试期望的行为在产品文档/设计稿中有明确说明
- [ ] 测试覆盖了边界情况和异常场景
- [ ] 测试命名清晰表达了意图（如 `should throw error when input is empty`）
- [ ] 测试断言的是业务行为，而非实现细节

### 测试不符合真实需求的迹象

- [ ] 测试硬编码了实现细节（如内部变量名、魔法数字、私有方法）
- [ ] 测试期望的行为与产品需求相矛盾
- [ ] 测试基于过时的需求或已被废弃的功能
- [ ] 测试过于具体，无法适应合理的实现变化
- [ ] 测试期望值与当前实际业务逻辑不一致

## 具体场景处理

### 场景 1：集成测试失败（API 响应、服务间调用）

```
❌ 错误做法：直接修改测试期望的响应格式/状态码
✅ 正确做法：
   1. 检查 API 契约/服务接口定义
   2. 确认期望的响应格式是否正确
   3. 如果契约要求不同的格式 → 修改 API/Service 实现
   4. 如果测试使用了过时的契约 → 更新测试
```

### 场景 2：单元测试失败（函数返回值、状态）

```
❌ 错误做法：修改测试断言使其通过
✅ 正确做法：
   1. 检查函数的功能需求文档
   2. 确认期望返回值是否有业务依据
   3. 如果业务逻辑要求不同的返回值 → 修改实现
   4. 如果测试期望不符合业务逻辑 → 修改测试
```

### 场景 3：测试环境问题（超时、文件缺失、网络错误）

```
✅ 正确做法：
   1. 区分"测试逻辑问题"和"测试环境问题"
   2. 环境问题：修复环境（创建缺失文件、启动服务）
   3. 如果环境无法修复：添加测试跳过逻辑并记录原因
   4. 不要因为环境问题就修改测试的业务断言
```

## 禁止行为

- ❌ **禁止**：为了使测试通过而修改测试断言
- ❌ **禁止**：添加 `test.skip()` 或 `pending` 来掩盖真实的测试失败
- ❌ **禁止**：修改测试的期望值以匹配错误的实现
- ❌ **禁止**：在未审查测试正确性的情况下修改代码

## 强制检查点

在修改任何测试之前，必须回答以下问题：

1. **这个测试覆盖了什么需求？**（引用需求文档或用户故事）
2. **测试期望的行为是产品应该有的行为吗？**
3. **如果我不改测试，产品行为会变成什么样？**
4. **修改测试是否会降低测试的保护作用？**

如果无法明确回答以上问题，**停止修改，先进行需求确认**。

## 记录要求

每次修复测试失败时，必须在提交信息中说明：

```
fix: 修复 XXX 功能

测试失败原因：测试期望 Y，但实际返回 Z
决策过程：
- 检查需求文档 XXX，确认期望行为应为 Y
- 当前实现返回 Z，不符合需求
- 修改实现代码（文件路径），使其返回 Y

测试未修改，因为测试期望符合产品需求。
```

或

```
fix: 修复 XXX 测试

测试失败原因：测试期望 Y，但产品实际行为是 Z
决策过程：
- 检查需求文档 XXX，确认产品行为应为 Z
- 测试期望 Y 不符合当前产品需求
- 修改测试期望值（文件路径），从 Y 改为 Z

原因：测试基于过时的需求/设计。
```

## 多服务架构调试规范

### 后端 404 错误调试流程

当多服务架构中遇到 404 错误时，不要假设"后端未运行"，按以下流程排查：

```
┌────────────────────────────────────────────────────────────────┐
│ 步骤 1：验证后端是否真正运行                                     │
│ - 检查进程：ps aux | grep -E "(uvicorn|fastapi|spring)"        │
│ - 直接请求：curl -I http://localhost:PORT/endpoint             │
│ - 确认不是"后端未运行"的误判                                    │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 2：检查实际 HTTP 请求 URL                                  │
│ - 记录失败请求的完整 URL                                        │
│ - 注意：URL ���能被反向代理/网关改写                              │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 3：对比路径一致性                                           │
│ Frontend Service URL → Proxy/Gateway → Backend Route           │
│ 三者必须完全对齐！                                              │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 4：检查 CORS/网络响应头                                     │
│ curl -I http://localhost:PORT/endpoint                          │
└────────────────────────────────────────────────────────────────┘
```

### 快速调试命令

```bash
# 检查后端进程
ps aux | grep -E "(uvicorn|fastapi|spring)" | grep -v grep

# 直接测试后端路由（最关键！）
curl -I http://localhost:PORT/endpoint

# 查看 proxy/gateway 配置
cat apps/server-java/gateway/src/main/resources/application.yml
```

### URL 路径对齐检查表

| 服务 | 后端服务 | 端口 | 前端调用 | Proxy/Gateway 目标 | Backend 路由 |
|---|---|---|---|---|---|
| AI Agents | Python | 8003 | /api/agents/* | http://localhost:8003/* | /agents/* |
| RAG | Java | 9001 | /api/rag/* | http://localhost:9001/* | /api/rag/* |
| Text Service | Python | 8006 | /api/text/* | http://localhost:8006/* | /text/* |
| TTS | Python | 8013 | /tts | http://localhost:8013/* | /tts/* |
| Vision | Python | 8000 | /api/vision/* | http://localhost:8000/* | /vision/* |
| Media Gen | Python | 8015 | /image/* | http://localhost:8015/* | /image/* |

### 常见错误诊断表

| 症状 | 可能原因 | 诊断方法 | 解决方案 |
|---|---|---|---|
| 404 | 后端未运行 | `curl -I http://localhost:PORT/` | 启动后端 |
| 404 | 路径不匹配 | 对比 frontend/proxy/gateway/backend | 统一路径 |
| CORS | origin 未列入 | `curl -I http://localhost:PORT/` | 添加到 allowed_origins |
| 502 | proxy target 错误 | 检查端口/目标地址 | 修正 proxy/gateway 配置 |

## 端口规范

### Java 服务
- Gateway: 9000
- RAG Service: 9001

### Python 服务
- AI Agents: 8003
- RAG (legacy): 8001
- TTS: 8013
- Vision: 8000
- Text Service: 8006
- Media Gen: 8015

### 前端服务
- Angular: 4200
