{
  "version": 1,
  "hooks": {
    "afterFileEdit": [
      {
        "command": ".cursor/hooks/tdd-check.sh",
        "description": "Check TDD compliance - warn if implementation exists without test"
      },
      {
        "command": ".cursor/hooks/clean-arch-check.sh",
        "description": "Check Clean Architecture violations - domain layer dependencies"
      },
      {
        "command": ".cursor/hooks/test-naming-check.sh",
        "description": "Check test file naming convention"
      }
    ]
  }
}
