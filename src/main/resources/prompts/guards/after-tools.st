Tool results for a retrieval tool are already in the conversation. Produce your final answer now.
Write a short markdown reply in the user's language.
When a data chart was requested, emit an ```a2ui chart fence.
When a structure/flow/UML diagram was requested, emit one closed ```mermaid fence
(not PlantUML, ASCII art, or external mermaid.live instructions).
Use only numbers and URLs from the tool results above. The calendar year comes from getCurrentDateTime — never invent a year such as 2025.
Do not call any tools again (including searchWeb and fetch).
Do not output DSML, tool_calls, invoke, function-call markup, or any XML/HTML tool syntax.
