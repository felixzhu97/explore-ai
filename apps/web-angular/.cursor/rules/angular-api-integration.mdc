---
description: Angular 与 Python 后端服务集成的调试规范和最佳实践。包含代理配置、CORS 设置、路由对齐等。
alwaysApply: true
---

# Angular 与 Python 后端集成规范

## 核心问题诊断流程

当 Angular 前端遇到 404、502、CORS 等错误时，按以下顺序排查：

```
错误现象
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 1：验证后端是否真正运行                                     │
│ - 检查进程：ps aux | grep uvicorn / fastapi                     │
│ - 直接请求：curl -I http://localhost:PORT/endpoint             │
│ - 确认不是"后端未运行"的误判                                    │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 2：检查实际请求 URL                                         │
│ - 打开浏览器 DevTools → Network 标签                           │
│ - 找到失败的请求，记录完整 URL                                  │
│ - 注意：URL 可能被 proxy.conf.json 改写                        │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 3：对比三方路径一致性                                       │
│                                                                   │
│ Frontend Service URL → proxy.conf.json → Backend Route         │
│                                                                   │
│ 示例：TTS 服务                                                   │
│ - Frontend 调用：/api/speech                                     │
│ - Proxy 目标：http://localhost:8001/speech                      │
│ - Backend 定义：@app.post("/tts") 或 @app.post("/speech")      │
│                                                                   │
│ 三者必须完全对齐！                                               │
└────────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ 步骤 4：检查 CORS 配置                                          │
│ - FastAPI CORSMiddleware 的 allowed_origins                    │
│ - 必须包含 Angular dev server 端口（默认 4200）                  │
│ - 示例：["http://localhost:4200", "http://localhost:5173"]     │
└────────────────────────────────────────────────────────────────┘
```

## 快速调试命令

```bash
# 1. 检查后端进程是否运行
ps aux | grep -E "(uvicorn|fastapi)" | grep -v grep

# 2. 直接测试后端路由（最关键！）
curl -I http://localhost:PORT/endpoint
# 例如：curl -I http://localhost:8001/tts

# 3. 检查 CORS 响应头
curl -I -H "Origin: http://localhost:4200" http://localhost:PORT/endpoint

# 4. 查看 proxy 配置映射
cat apps/web-angular/proxy.conf.json
```

## URL 路径对齐检查表

### 常见服务端口和路由

| 服务 | 端口 | 前端调用路径 | Proxy 目标 | Backend 路由 |
|------|------|-------------|------------|-------------|
| AI Agents | 8000 | /api/agents/* | http://localhost:8000/* | /agents/* |
| RAG | 8002 | /api/rag/* | http://localhost:8002/* | /rag/* |
| TTS | 8001 | /api/speech | http://localhost:8001/tts | /tts 或 /speech |
| Vision | 8003 | /api/vision | http://localhost:8003/vision | /vision 或 /analyze |

### 对齐检查流程

```
1. Frontend (Angular Service)
   → this.http.post('/api/speech', body)

2. proxy.conf.json
   → "/api/speech": { "target": "http://localhost:8001" }
   → 注意：是否需要 rewrite？

3. Backend Route (FastAPI)
   → @app.post("/tts", ...)  ← 必须与 proxy 后的路径一致
   OR
   → @app.post("/speech", ...)  ← 如果 proxy 不 rewrite
```

## proxy.conf.json 最佳实践

```json
{
  "/api": {
    "target": "http://localhost:8000",
    "secure": false,
    "changeOrigin": true,
    "logLevel": "debug"
  },
  "/api/speech": {
    "target": "http://localhost:8001",
    "secure": false,
    "pathRewrite": { "^/api/speech": "/tts" }
  },
  "/api/vision": {
    "target": "http://localhost:8003",
    "secure": false,
    "pathRewrite": { "^/api/vision": "/vision" }
  }
}
```

## CORS 配置模板

```python
# FastAPI CORS 配置
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI()

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:4200",  # Angular dev
        "http://localhost:5173",  # React dev
        "http://localhost:3000",  # Next.js dev
        # 生产环境域名
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## 常见错误诊断

### 404 Not Found

| 原因 | 诊断方法 | 解决方案 |
|------|---------|---------|
| 后端未运行 | `curl -I http://localhost:PORT/` | 启动后端服务 |
| 路径不匹配 | 对比三方路径 | 修改 frontend/proxy/backend 任一 |
| Proxy 未生效 | 检查 angular.json 是否引用 | 在 angular.json 中添加 proxyConfig |

### CORS 错误

```
Access to fetch at 'http://localhost:8001/tts' from origin 
'http://localhost:4200' has been blocked by CORS policy
```

| 原因 | 诊断方法 | 解决方案 |
|------|---------|---------|
| origin 未列入 | curl -I 检查 Vary: Origin | 添加到 allowed_origins |
| 方法未允许 | 检查 allow_methods | 改为 ["*"] |
| 头部未允许 | 检查 allow_headers | 改为 ["*"] |

### 502 Bad Gateway

| 原因 | 诊断方法 | 解决方案 |
|------|---------|---------|
| Proxy target 错误 | 检查端口是否正确 | 修正 proxy.conf.json target |
| 后端崩溃 | 检查后端日志 | 重启后端服务 |

## 调试工作流总结

```
┌─────────────────────────────────────────────────────────────────┐
│                    Angular Backend 调试流程                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. 确认后端运行     ps aux | grep uvicorn                       │
│          ↓                                                         │
│  2. 直接 curl 测试   curl -I http://localhost:PORT/path          │
│          ↓                                                         │
│  3. 浏览器 DevTools  查看实际请求 URL 和响应                      │
│          ↓                                                         │
│  4. 检查 proxy.conf  对比 frontend 调用 vs proxy 目标            │
│          ↓                                                         │
│  5. 检查 backend 路由 对比 proxy 目标 vs @app.post()             │
│          ↓                                                         │
│  6. 检查 CORS        curl -I -H "Origin: ..." 验证              │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```
